[project]
name = "officiating-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.4",
    "python-decouple>=3.8",
    "python-dotenv>=1.1.1",
]
