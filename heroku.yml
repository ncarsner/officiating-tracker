build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - uv run python manage.py migrate
    - uv run python manage.py collectstatic --noinput
run:
  web: uv run gunicorn project.wsgi --bind 0.0.0.0:$PORT --log-file -
